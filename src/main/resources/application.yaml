server:
  port: 8082
spring:
  application:
    name: personal-organization
  data:
    mongodb:
      #authentication-database: admin
      #uri: mongodb://root:example@localhost:27017/organization?authSource=admin&authMechanism=SCRAM-SHA-256
      uri: mongodb+srv://${MONGODB_USER}:${MONGODB_PASSWORD}@cluster0.q9xza8m.mongodb.net/organization?retryWrites=true&w=majority&appName=Cluster0&ssl=true&tls=true
      #host: localhost
      # port: 27017
      # database: mudanca
      # username: root
      # password: example

